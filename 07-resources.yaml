apiVersion: v1
kind: Pod
metdata:
  name: resources
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "64mi"
        cpu: "100m"
      limits:
        memory: "128mi"
        cpu: "100m"
